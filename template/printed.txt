set guifont=Monospace\ 22
set termguicolors
set number relativenumber
set tabstop=4 shiftwidth=4
set clipboard=unnamedplus
set noswapfile
set undofile
set undodir=~/.vim/undodir
set foldmethod=syntax
set autoindent smartindent cindent
syntax on
colorscheme shine
filetype plugin indent on
highlight Number ctermfg=magenta guifg=#ee77ee
highlight MatchParen guifg=NONE guibg=NONE cterm=NONE
highlight Folded ctermbg=NONE guibg=NONE
inoremap { {<CR>}<Esc>O
inoremap ( ()<Left>
inoremap [ []<Left>
inoremap " ""<Left>
inoremap ' ''<Left>
inoremap ,cpp <Esc>:0r ~/template<CR>:silent! /\/\/cursor<CR>:nohlsearch<CR>cc
augroup numbertoggle
autocmd InsertEnter * set norelativenumber
autocmd InsertLeave * set relativenumber
augroup END
augroup viewsave
autocmd BufWinLeave ?* silent! mkview
autocmd BufWinEnter ?* silent! loadview
" Save, compile, and run in a vertical terminal split
autocmd filetype cpp nnoremap <F5> :w <CR> :!g++ -std=c++17 -O2 -Wall % -o %< <CR> :vert term ./%< <CR>

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> pii;
typedef vector<int> vi;
typedef vector<ll> vll;

#define all(x) (x).begin(), (x).end()
#define pb push_back
#define fi first
#define se second
#define rep(i, a, b) for (int i = (a); i < (b); i++)

const int MOD = 1e9 + 7;
const int INF = 1e9;
const ll LINF = 1e18;

template <typename T>
void read(vector<T> &a)
{
    for (auto &x : a)
        cin >> x;
}
    /* if ez:
    ll n;
    cin >> n;
    vll arr(n);
    read(arr);
   */
void solve()
{
	//cursor

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1;
    //cin >> t;
    while (t--)
        solve();

    return 0;
}
