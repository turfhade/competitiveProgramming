#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vll = vector<ll>;

int main()
{
    ll n;
    vll in(n+1);
    for(ll i = n; i >= 0; i--)
    {
        cin >> n[i];
    }
    while(in.size() != 1 && in[0] != 1)
    {
      
    }
    return 0;

}